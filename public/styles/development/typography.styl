$minFontSize = 2.5em
$maxFontSize = 3em

::marker
  color _color2

.main-heading
  word-wrap break-word
  //font-size 'clamp(%s, %s, %s)' % ($minFontSize unit($maxFontSize, 'vw') $maxFontSize)
  text-shadow: .2em .2em .2em rgba(0, 0, 0, .3) // offset-x | offset-y | blur-radius | color

.main-heading span
    color _color2

//.separator
//  width 100%
//  height 10em
//  fill currentColor
//  margin 2em auto

hr[data-hr]
.decorative-hr hr
  display block
  //clear both
  width 100%
  height 2em
  margin 2em 0
  //font-family _fontList2
  text-align center
  line-height 1
  //letter-spacing .5em
  color rgba(255, 255, 255, .2)
  background-image none

hr[data-hr]::after
.decorative-hr hr::after
  content attr(data-hr)
  display block
  width 100%
  height 100%
  font-size 2em

.decorative-hr hr::after
  content '✻ ✻ ✻'

//:lang(grc) // @note Grec ancien (grec moderne : 'el')
//  color orange

:lang(he) // @note Hébreu
    direction rtl

.verse
  color _color2
