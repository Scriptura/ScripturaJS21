[class*=message]
  margin-bottom 1em
  padding 0 1em
  background-color _colorG + 7%
  box-shadow _shadow5
  overflow hidden // @note Evite un conflit avec les margins et padding des éléments enfants.

main [class*=message] // @note Seulement pour les messages dans le main. Ailleurs, utiliser le style global.
  border-radius _r2

p[class*=message]
  padding 1em

.message-primary
  background-color _colorG + 20%

.message-secondary
  background-color _colorG + 3%

.message-info
  background-color _color6

.message-success
  background-color _color4

.message-error
  background-color _color3

.message-warning
  color invert(_colorT)
  background-color _color5
